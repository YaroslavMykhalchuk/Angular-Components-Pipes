import { RemoveDublicatePipe } from './remove-dublicate.pipe';

describe('RemoveDublicatePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDublicatePipe();
    expect(pipe).toBeTruthy();
  });
});
