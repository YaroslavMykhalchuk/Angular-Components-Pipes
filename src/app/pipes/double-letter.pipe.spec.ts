import { DoubleLetterPipe } from './double-letter.pipe';

describe('DoubleLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
